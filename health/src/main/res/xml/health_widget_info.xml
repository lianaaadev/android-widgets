<?xml version="1.0" encoding="utf-8"?>
<!--
  No android:configure. There is one metric and units are app-wide, so a widget dropped from the
  picker is already fully specified. The config activity arrives in Phase 4 with the accent
  picker, which is the first thing that genuinely varies per widget.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_description"
    android:initialLayout="@layout/glance_default_loading_layout"
    android:minHeight="110dp"
    android:minResizeHeight="110dp"
    android:minResizeWidth="110dp"
    android:minWidth="110dp"
    android:previewLayout="@layout/widget_preview"
    android:resizeMode="horizontal|vertical"
    android:targetCellHeight="2"
    android:targetCellWidth="2"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen" />
