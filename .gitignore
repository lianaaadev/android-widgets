# Android / Gradle
*.iml
.gradle/
local.properties
.idea/
build/
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
*.keystore
!debug.keystore

# macOS
.DS_Store

# Design mockups: the .dc.html artboards, the canvas manifest, and the 2.5 MB seeded
# canvas. Kept locally, out of the repo. The published canvas is the source of record,
# and docs/*.png are the exported stills the README uses.
mockup/
